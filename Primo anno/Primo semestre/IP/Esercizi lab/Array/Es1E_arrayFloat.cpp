// Es_1 di riscaldamento: creaArrayFloat, Crea un array di prova con elementi di tipo float. Scrivere un programma che dichiara un array v di N float
// e lo “popola” (assegna valori ai suoi elementi).

#include <iostream>

using namespace std;

int main()
{
	// Dichiarare una costante N con valore 10
	const int N = 10;
	// Dichiarare un array a di N interi
	float v[N];
	// Iterare sulla variabile intera i a partire da 0 e fino a N escluso:
	for (int i=0; i<N; i++){
	// Assegnare all'elemento i-esimo di a il valore N-i
		v[i] = N-i;
		cout << a[i] << endl;
		}

}

	// 1,2,4,6,8,12,17
